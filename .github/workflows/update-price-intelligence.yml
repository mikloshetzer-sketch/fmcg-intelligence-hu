name: Automated Price Intelligence Update

on:

  schedule:

    - cron: '0 4 * * *'

  workflow_dispatch:

permissions:

  contents: write

jobs:

  update-price-intelligence:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository

        uses: actions/checkout@v4

      - name: Set up Python

        uses: actions/setup-python@v5

        with:

          python-version: '3.11'

      - name: Install dependencies

        run: |

          python -m pip install --upgrade pip

      - name: Run Price Intelligence updater

        run: |

          python scripts/update-price-intelligence.py

      - name: Commit changes

        run: |

          git config user.name github-actions

          git config user.email github-actions@github.com

          git add docs/data/price-history.json

          git commit -m "Automated Price Intelligence update" || echo "No changes"

          git push
